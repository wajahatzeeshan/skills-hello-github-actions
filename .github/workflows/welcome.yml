name: Post welcome comment

on:
  pull_request:
    types: opened

permissions:
  pull-requests: write

jobs:
  welcome:
    name: Post welcome comment
    runs-on: ubuntu-latest
    steps:
      - run: |
          gh pr comment "$PR_URL" --body "Hi @${{ github.event.pull_request.user.login }}! 👋

          Thanks for opening a pull request! We appreciate your contribution and will review it as soon as possible.

          In the meantime, feel free to check out our contributing guidelines and code of conduct.

          Happy coding! 🚀"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}